# DEPLOY-4: Production build target.
# In CI, the GitHub Actions workflow overrides this with the secret VITE_API_BASE_URL
# so the deployed SPA points at the Tailscale FQDN of the homeserver.
# Example: VITE_API_BASE_URL=https://homeserver.tail<XXXXX>.ts.net
VITE_API_BASE_URL=
